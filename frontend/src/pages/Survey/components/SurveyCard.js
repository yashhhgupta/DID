const SurveyCard = ({ survey }) => {
    const {
        title,
        description,
        participation,
        deadline,
        status,
        created_at,
        inclusionScore,
    } = survey;
    return (
        <div>SurveyCard</div>
    ) 
}
export default SurveyCard;